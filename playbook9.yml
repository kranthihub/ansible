---
- name: creating users by single server
  hosts: 172.31.93.237

  tasks: 
     - name: create user 
       user: 
          name: "{{a}}"
          password: "{{b}}"
          uid: "{{c}}"
          home: "{{d}}"
          shell: "{{e}}"
          comment: "{{f}}"
...            
